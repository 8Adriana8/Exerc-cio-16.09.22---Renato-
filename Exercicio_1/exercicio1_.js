/** Dado o tamanho da base e da altura de um retangulo, calcular a sua área e o seu perímetro  */

let base = Number(prompt("Inserir valor da base do retangulo"))
let altura = Number(prompt("Inserir o valor da altura do retangulo"))
let calculo = base * altura
alert("valor da area do triangulo: " + calculo)

calculo = 2 * (base + altura)

alert("valor do perimetro do triangulo: " + calculo)
